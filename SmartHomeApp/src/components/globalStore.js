// for the server ip if using android emulator the ip should be 10.0.2.2, if using actual phone ip should be the ipv4 of the computer server is hosted on
const globalStore = {serverIP: '10.0.2.2'};

// global store used for globals vars, mainly for sever ip and some mqttclient instances
export default globalStore;
